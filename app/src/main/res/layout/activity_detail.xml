<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="1">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView android:text="Type:" android:textStyle="bold"/>
            <TextView android:id="@+id/tvType" android:layout_marginBottom="12dp"/>

            <TextView android:text="Name:" android:textStyle="bold"/>
            <TextView android:id="@+id/tvName" android:layout_marginBottom="12dp"/>

            <TextView android:text="Phone:" android:textStyle="bold"/>
            <TextView android:id="@+id/tvPhone" android:layout_marginBottom="12dp"/>

            <TextView android:text="Description:" android:textStyle="bold"/>
            <TextView android:id="@+id/tvDesc" android:layout_marginBottom="12dp"/>

            <TextView android:text="Date:" android:textStyle="bold"/>
            <TextView android:id="@+id/tvDate" android:layout_marginBottom="12dp"/>

            <TextView android:text="Location:" android:textStyle="bold"/>
            <TextView android:id="@+id/tvLocation" android:layout_marginBottom="12dp"/>
        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/btnRemove"
        android:text="REMOVE"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/bg_button"/>
</LinearLayout>
